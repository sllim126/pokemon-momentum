Pokemon Momentum Dashboard: How To Use

Recent Implementation Notes (March 2026)
----------------------------------------
This section documents behavior that changed during the latest dashboard refinement pass so
new reviewers can verify expected UX quickly.

1. Top ticker behavior
- The old left-rail mover list has been transformed into a top ticker-style market band.
- Ticker cards are intentionally compact and continuously scroll.
- Clicking a ticker item immediately selects that product and loads the main chart.
- Expected result:
  - users can scan momentum quickly without losing main chart focus
  - first interaction path is faster and more obvious

2. Market Signals tab click behavior
- When a signal tab is selected, the table loads and the first row is auto-selected.
- The main chart then loads from that first row automatically.
- Expected result:
  - no extra click is required after switching tabs
  - each signal tab is immediately actionable

3. Loading feedback
- Market Signals table now shows an explicit loading spinner overlay while requests are in flight.
- Selected chart already shows loading spinner/state during series fetch.
- Expected result:
  - users see active feedback instead of a static screen during slower queries

4. Selected product image placement
- The selected item image now appears inside the Selected Product header, left of the product title.
- The prior standalone image card in the right rail was removed.
- Expected result:
  - product context (image + name + price) is grouped together near the chart

5. Tracking actions placement
- `Favorite`, `Watchlist`, `Research`, and `Buy List` moved into a dedicated right-rail
  `Track This Item` card.
- `Tracked Items` action is colocated there.
- Expected result:
  - top header remains navigation/search focused
  - item-level actions remain visible but unobtrusive

6. Browse Set name resolution
- `Browse Set` now depends on server-provided names for display.
- Additional fallback logic handles stale placeholder names (`Product <id>`).
- Expected result:
  - significantly fewer generic placeholder names in set tables

7. Mobile-first dashboard behavior
- phone/tablet layouts now use a bottom nav:
  - `Home`
  - `Search`
  - `Tracked`
  - `Indicators`
- selected product opens as a mobile drilldown page instead of permanently occupying the home screen
- the mobile home screen now includes a `Singles / Sealed` toggle
- pull-to-refresh now refreshes the current dashboard state without requiring a full browser reload

8. In-page image preview
- clicking or tapping a product image now opens a small in-page preview anchored over that image
- tapping the enlarged preview again closes it
- this works on both desktop and mobile

9. Signals screen removed
- the old `Signals` tab has been removed from the dashboard UI for now
- the remaining indicator set is now more focused on actionable momentum and set-research screens

10. Search-to-Browse-Set handoff
- header search now treats set/product results more like navigation than a raw jump
- searching for a set opens `Browse Set` for that set
- searching for a product can also open `Browse Set` for that product's set and select the exact item
- expected result:
  - search feels more like "take me to the right set context"
  - users do not have to manually switch tabs after finding a result

11. Browse Species and search refinement
- broad card-name searches can now open `Browse Species` instead of dropping immediately into one set
- `Browse Species` can also be opened from the currently selected card without typing a new search
- expected result:
  - searching for a species like `Seedot` or `Greninja` can show the cross-set card list directly
  - `Browse Set` stays focused on set drilldown

12. Good Buys refinement
- `Good Buys` now requires at least `10` distinct prices in the last `30` days
- the screen now supports a `Filter Sets` popout so only specific sets can be reviewed
- expected result:
  - fewer illiquid or overly special-case names in the list
  - easier review of normal premium-rarity pullback candidates from just the sets you care about

13. Quick Buy Targets
- selected-card details now show a quick bargaining breakdown
- target price references are shown at `95%`, `90%`, `85%`, `80%`, `75%`, `70%`, `65%`, and `60%` of the latest market price
- expected result:
  - faster buying decisions at shows or in quick sourcing situations

What This Is
This dashboard is a research and discovery tool built around Pokemon market data.
It takes a large amount of price history and turns it into something easier to scan, compare, and act on.

Instead of digging through raw exports, price pages, or disconnected spreadsheets, this site puts the most important signals in one place.
It helps answer simple but important questions:
- what is moving right now
- what may be starting to move before it becomes obvious
- which sets are showing broad strength
- whether cards and sealed products are behaving differently

Why We Have It
The main purpose of this site is to make decision-making faster and clearer.
It was created to help spot momentum earlier, reduce noise, and make it easier to focus on the products that actually deserve attention.

For the owner, it helps:
- identify cards or sealed products that may be trending upward
- compare movement across products and sets more quickly
- build a repeatable research process instead of relying on memory or guesswork
- connect market insight to buying, selling, and inventory decisions

For visitors, it helps:
- show what is active in the market in a clear visual way
- surface products they may not have thought to check
- make price movement easier to understand at a glance
- create a more useful and engaging experience than a static catalog alone

Set Explorer
There is now also a lighter companion page for set browsing:
- `/set-explorer`

This page is for curiosity and comparison rather than signal hunting.
It is meant to answer questions like:
- which sets are most expensive to build from tracked cards
- which sets are carried by one or two chase cards
- which sets have value spread more broadly across the checklist
- what a set looks like visually when a local set logo has been uploaded

It does not replace the main dashboard.
Instead, it gives a simpler place to poke around in the set data without learning the chart and screener workflow first.

Overview
This dashboard is designed to help you scan for momentum across Pokemon cards and sealed products, then drill into a single item quickly.
The layout is split into three main areas:
- left rail
- main chart and signal feed
- right rail


Left Rail: Trending Up
The left rail is your fast watchlist.
It shows the current top movers for the selected segment:
- Cards
- Sealed

Each card in the rail includes:
- product name
- set name
- subtype
- current price
- 30-day move
- mini price trend sparkline

What to do here:
- click a mover card to load it into the main chart
- switch between Cards and Sealed to see a different momentum feed
- use it as a fast shortlist of names worth reviewing in more detail

The mini graph is only meant to be a quick visual trend summary.
The full detail view always lives in the main chart area.


Main Chart Area
The center of the dashboard is the detailed view for the currently selected item.
This is where you spend most of your time once something catches your attention.

The top of the chart area shows:
- selected product name
- set and subtype
- current price
- latest data date

The large chart shows:
- daily price history
- SMA7
- SMA30

Chart scaling notes:
- cards under `5` keep Plotly auto-range
- longer windows can switch to visible price-window bounds when the chart clearly moved into a new price regime
- 30D charts can also widen just enough when:
  - the visible month trades below the opening level
  - or SMA30 would otherwise get squeezed off the chart on a fast mover

Chart window buttons
These buttons change how much history is shown:
- 30D: last 30 days
- 90D: last 90 days
- 180D: last 180 days
- 1Y: last 365 days
- 2Y: last 730 days
- Full: full available history

Use short windows when you want to see recent acceleration.
Use longer windows when you want to understand the broader trend.

Important behavior:
- the dashboard now keeps your current state in the URL
- refreshes preserve the selected product, market, chart window, signal tab, rail segment, and selected set
- this makes it easier to keep your place while reviewing many cards


Signal Feed
Below the main chart is the signal feed and product browser area.
This section is used to scan lists of opportunities and then click through to a specific name.

Tabs in the signal feed:

Top Movers
Shows the strongest recent movers by 30-day change.
This is useful for seeing what is already moving the most.

Breakouts
Shows products making a real fresh break above their prior 90-day high.
This now also prefers names with repeated distinct prices in the last 30 days, which helps filter out flat cards merely sitting at the top of a range.
This screen also respects the shared `Cards / Sealed` toggle, so booster boxes should only appear when `Sealed` is selected.

Good Buys
Shows premium-rarity cards that have been trending downward or pulling back.
This is meant to surface high-end cards that may be moving into more interesting buy zones rather than already spiking.
The first version focuses on rarities like Double Rare, Illustration Rare, Special Illustration Rare, Ultra Rare, Hyper Rare, and Secret Rare.
This screen now also requires at least `10` distinct prices over the last `30` days, and it includes a `Filter Sets` popout so you can narrow the review to just the sets you care about.

SMA30 Holds
Shows products that reclaimed and held above the 30-day moving average.
This is useful for finding names that may be stabilizing into a trend.

Early Movers
Shows names where short-term momentum is starting to line up.
This is meant to catch products that may be moving upward before they become obvious spikes.

Confirmed Movers
Shows more confirmed momentum names.
These are generally stronger trend candidates than the earliest setups.

Set Strength
Shows set-level breadth and momentum.
This is helpful when you want to know which sets are strong overall, not just which single card is moving.
This view also supports generation filtering so you can narrow the set list to eras like SV, SWSH, SM, XY, and older groups.
Japanese Mega-era sets are intentionally grouped into `MEG` even when the prefixes vary (`ME`, `M2`, `M2a`, `M3`, `MBD`, `MBG`, `MP1`, etc.).
When a local set logo exists in the uploaded logo folder, the right-side image panel can show that logo instead of the generic set-view placeholder.

Browse Set
Shows all products in the currently selected set from the Set Picker dropdown.
Use this when you want to browse one set directly rather than only relying on the screener tabs.
Header search also uses this screen as the destination for set-oriented searches, so a set search now opens directly into `Browse Set`.

Browse Species
Shows matching listings for the same searched card name across sets.
Use this when you want to compare one species or card name across many sets instead of drilling into only one set.

Simple one-line summaries
- `Early Movers`: the earliest momentum names starting to turn positive
- `Confirmed Movers`: stronger and more confirmed trend names
- `Top Movers`: the biggest recent movers over the last 30 days
- `Consistent Movers`: cards steadily holding above SMA30 with real movement
- `Good Buys`: premium cards that have pulled back into more interesting buy zones
- `Set Strength`: whole-set strength and participation
- `Browse Set`: drill directly into one set and browse all tracked cards/products inside it
- `Browse Species`: compare one card name across many sets

Browsing behavior:
- the lower table is now its own scroll region, so the main chart stays visible while you review long lists
- you can step through the current table with the keyboard:
  - Arrow Down selects the next row
  - Arrow Up selects the previous row
- selecting a set automatically switches to the set view and loads the first product in that set
- searching for a set opens `Browse Set` automatically
- searching for a specific card or sealed item can also open `Browse Set` for that set with that exact item selected


What the Indicators Mean
These indicators are simple momentum tools, not guarantees.
They are meant to help you narrow attention.

SMA7
7-day simple moving average.
This is a short-term trend line.

SMA30
30-day simple moving average.
This is a medium-term trend line.

Price above SMA30
Usually means the item is trading above its recent average.
That is often a constructive trend sign.

SMA7 above SMA30
Usually means short-term trend is stronger than medium-term trend.
That is a stronger momentum confirmation.

Early Uptrend idea
The dashboard uses short-term trend alignment to surface names that may be beginning to move up without already being fully extended.

Breakout
A breakout generally means the product is reaching or exceeding a recent high window.
That can indicate strength, but it can also mean the move is already more mature.

Top Movers
This is mostly a ranking view.
It tells you what already moved the most over the selected lookback logic.

Set Strength
These are set-level breadth metrics.
They help answer whether strength is isolated to a few cards or spread across a set.

Set Chart From Set Strength
When you click a row in Set Strength, the main chart can switch into a set-level view instead of only showing one card.

That set chart currently shows:
- Set Index
- % Above SMA30

Set Index
This is a normalized equal-weight set index.
Each tracked item in the set is rebased to 100 at the start of the selected window, then averaged together.
This helps show how the set is moving as a whole without letting one expensive chase card dominate the chart.

Why it starts at 100:
- `100` is the baseline at the start of the selected window
- above `100` means the set is up from that starting point
- below `100` means the set is down from that starting point
- this makes it easier to compare how sets perform over time without using raw dollar prices

% Above SMA30
This is daily breadth.
It shows what percent of tracked items in the set are trading above their own 30-day moving average on each day.

How to read the set chart:
- rising Set Index + rising % Above SMA30
  usually means broad set strength
- rising Set Index + weak % Above SMA30
  usually means a narrower move driven by fewer cards
- flat Set Index + rising % Above SMA30
  can mean internal participation is improving before the set price picture looks obviously strong

Plain-English shortcut:
- blue line = what the set is doing overall
- green line = how many cards inside the set are helping

Quick cheat sheet:
- blue up + green up
  strongest setup
  the set is rising and more cards are participating
- blue up + green down
  caution
  the set is rising, but fewer cards are helping
- blue flat + green up
  early improvement
  the set is sideways, but more cards are starting to act better
- blue flat + green down
  weak setup
  the set is sideways, but fewer cards are holding up
- blue down + green up
  mixed / possible stabilization
  the set is still weak overall, but more cards are starting to recover
- blue down + green down
  weakest setup
  the set is falling and participation is getting worse


Set Explorer Page
The Set Explorer is a separate page built for set-level questions.
It uses snapshot data rather than full chart analysis, so it is quicker to understand at a glance.

The main ideas on that page are:

Tracked set cost
- the sum of the latest tracked card prices in the set explorer basket
- this is an estimate, not an exact TCGplayer cart optimizer total

Top hit share
- what percent of the tracked set cost comes from the single most expensive card

Top 3 share
- what percent comes from the three most expensive cards

Depth score
- a simple inverse concentration read
- lower concentration means more of the set's value is spread across many cards

Typical use:
1. open `/set-explorer`
2. sort by most expensive, cheapest, most top-heavy, or deepest
3. click a set row
4. use the spotlight panel to see whether that set feels broad or chase-card driven
5. if a local set logo has been uploaded and synced, the spotlight panel will show it before falling back to top-hit card art


Card Picker
The Card Picker dropdown lets you jump directly to a specific product.
Use this when you already know what card or sealed item you want to inspect.

Selecting a product here updates:
- main title
- image
- chart
- detail summary and tracking controls


Set Picker Dropdown
The Set Picker dropdown selects which set is used for the `Browse Set` view.
When `Browse Set` is active, the table will show all products from the selected set.

Typical use:
1. choose a set in the Set Picker dropdown
2. the dashboard automatically switches to the set product list
3. the first product in the set is loaded into the main chart
4. review the rest of the set in the lower list and click or arrow through rows


Market Dropdown
The Market dropdown switches between available data universes.
Current options include:
- Pokemon
- Pokemon Japanese

Changing market updates the dashboard queries so you are working inside that specific market.
If something appears empty after switching, it usually means that feed has little or no qualifying data in that market yet.


Selected Product Detail
The selected product detail area is the context stack for the currently selected product.
It is meant to give context without leaving the chart.

Sections include:
- selected product image
- tracking / favorites
- key stats
- chart context

Tracking / Favorites
This is the lightweight tracking area.
It lets you mark items such as:
- Favorite
- Watchlist
- Research
- Buy List

Buy List is meant for cards and products you want to source soon, especially for card shows or targeted shopping runs.

How it works now:
- if you stay signed out, tags are saved locally in that browser only
- if you sign in with a tracking username and PIN, tags sync to the tracking account and follow you between sessions and devices

This is intentionally not a full collection tracker.
It does not currently track:
- quantities
- cost basis
- profit and loss
- portfolio totals

It is meant to save lightweight workflow states only.

Account Menu
The account controls now live in the upper-right header area, which is where most users expect sign-in controls to be.

When signed out:
- click `Account`
- enter username and PIN
- choose:
  - `Sign In`
  - `Create Account`

When signed in:
- click your username in the header
- open:
  - `Tracked Items`
  - `Account Settings`
  - `Sign Out`

Tracked Items
`Tracked Items` is the saved-items list available from the account menu on desktop and the bottom navigation on mobile.
It shows the products you marked with tracking tags and lets you filter by:
- All Tags
- Favorite
- Watchlist
- Research
- Buy List

Opening `Tracked Items` will automatically load the first tracked card into the main chart so the screen is immediately usable.

Key Stats
This shows the current summary numbers for the selected item, such as:
- set
- subtype
- rarity
- card number
- latest price
- SMA7
- SMA30
- price versus SMA30

Selected Product Image
The selected card or sealed product image lives in the selected-product detail header.
This keeps the art close to the title, chart, and tracking actions.
If you click that image, it opens a small in-page preview anchored over the current image.
Clicking or tapping the enlarged preview again closes it.


Suggested Workflow
1. Start on the left rail and click anything interesting.
2. Review the main chart and compare price vs SMA7 and SMA30.
3. Use the signal tabs to look for confirmation or earlier-stage setups.
4. Use Browse Set to inspect neighboring items in the same set.
5. Use the tracking controls to tag and keep track of names you want to revisit.
6. If you want those tags on another device, sign in to the tracking account and let them sync.


Important Reminder
This dashboard is a research and screening tool.
It helps surface patterns and candidates.
It does not guarantee that a card or product will continue moving higher.


What To Look For If You Want To Buy Early
If you are trying to find cards or products that are just starting an upward trend, the goal is usually to find real strength before the move becomes obvious to everyone else.

The best early signs are often:
- price above SMA30
- SMA7 above SMA30
- SMA3 above SMA7 above SMA30
- several recent positive price changes
- actual movement within the last few days
- price not already too far extended above trend

In practical terms, you usually want something that is:
- starting to trend upward
- showing repeated activity
- not already fully spiked

Best places to look in this dashboard:

Early Movers
This is usually the best first place to look for early setups.
It is designed to surface names where short-term momentum is starting to line up before they become obvious top movers.

SMA30 Holds
This is useful for finding names that reclaimed and are holding above their 30-day trend.
It can help identify products that may be stabilizing into an uptrend.

Confirmed Movers
This is a stronger confirmation list.
These names may be more developed than the earliest setups, but they are often higher-confidence trends.

Set Strength
This helps you see whether a whole set is strengthening.
If a set is broadening, more products inside it may start to follow.

Simple early-trend checklist
- appears in Early Movers
- price is above SMA30
- SMA7 is above SMA30
- there were real price changes in the last 3 to 5 days
- the product is not already massively extended
- ideally the set is also improving in Set Strength

What to be careful of
- one-day spikes with no follow-through
- stale items that moved once and then stopped
- sparse-history items with unreliable pricing
- products that are already extremely far above SMA30

The goal is not to buy everything that is moving.
The goal is to find products that are beginning to trend upward while there is still room for the move to develop.
