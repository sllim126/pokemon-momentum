Pokemon Momentum TODO
=====================

Recently Completed (March 2026)
-------------------------------

- Top ticker / marquee polish completed for now.
  - moved to top broadcast-style band
  - compact item payload and fast sparkline behavior
  - click-through selection to main product context
- Added visible loading states in Market Signals.
  - table now shows an explicit spinner overlay while screener data is loading
  - chart already shows loading state while selected series is fetched
- Market Signals interaction improvement:
  - clicking indicator tabs now auto-selects the first returned row
  - main chart loads immediately without requiring a second click
- Moved product image into the selected-product header area (left of title) for faster context.
- Tracking actions moved into a dedicated right-rail `Track This Item` card.
- Removed forced full-universe browser load in set-products flow.
  - chart selection can resolve from `currentKey` directly
  - set-products naming now resolved server-side
- Added server/client fallback logic for placeholder names (`Product <id>`).
  - server `group_products` now falls back to product-signal names when metadata name is placeholder
  - client row normalization now prefers non-placeholder payload names
- Moved the left-rail mover list into a top ticker / marquee band.
- Added explicit loading states for chart and Market Signals table fetches.
- Added a first server-side search pass and header search UI.
- Reduced some forced full-universe loads so card/set detail can load more on demand.
- Added `Browse Species` as a research tool for cross-set card-name browsing.
- Added `Quick Buy Targets` to the selected-card details for fast bargaining references.
- Reworked `Good Buys` set filtering into a popout checklist over the results area.
- Renamed momentum labels for clarity:
  - `Early Movers`
  - `Confirmed Movers`
- Mobile dashboard pass:
  - bottom nav now drives `Home`, `Search`, `Tracked`, and `Indicators`
  - selected-product view is a mobile drilldown instead of always occupying the home screen
  - `Singles / Sealed` mobile home toggle added
  - pull-to-refresh added for phone / tablet layouts
  - tapping images now opens a small in-page preview anchored over the clicked image
- Removed the old `Signals` indicator from the dashboard UI for now.
  - it can be added back later if it earns a clearer purpose

Next Up
-------

- Profile `/dashboard` in browser developer tools and write down the current slowest first-load requests.
- Reduce first-render time by deferring any non-essential API calls still blocking the first usable paint.
- Review current header search behavior and fix any remaining result-selection quirks.
- Improve search ranking for broad card names and edge cases.
- Test the dashboard on:
  - iPhone / phone
  - iPad / tablet
  and write down the concrete layout problems instead of keeping this as a generic mobile task.
- Review `/embed` on phone and tablet after the main mobile layout settles.
- Add a first-time guided tour / onboarding flow for new users.
- Make the onboarding explain the main Market Signals buttons in plain language.
- Tighten `Good Buys` so it keeps premium cards that are weakening but not obviously broken.
- Decide whether `Good Buys` should prefer cards flattening over the last 7 days instead of still actively falling.
- Tighten `Top Movers` and `Early Movers` so stale one-sale jumps are less likely to pass.
- Expand Set Explorer rarity / variant filters beyond the current first pass.
- Fix any remaining generation grouping mistakes when a set clearly lands in the wrong era bucket.
- Add camera / photo card lookup for show-floor use.
- Extend tracking into:
  - `/embed`
  - `/set-explorer`
- Add keyboard shortcuts beyond the current up/down row stepping if they clearly improve desktop workflow.
- Add a Discord webhook / bot path so a card can be requested and its key stats returned as a response.

Needs Decisions
---------------

- Decide whether mobile should keep the current three-panel structure or move to a more task-based stacked layout.
- Decide whether the right rail should stay a side column on smaller screens or collapse into a more mobile-first detail flow.
- Decide whether the left rail should remain a side column or fully become a stock-style ticker / trending bar at the top.
- Decide how prominent the Market Signals block should stay relative to the main chart on mobile/tablet.
- Decide whether the guided tour should be:
  - public-facing only
  - optional for returning users
  - dismissible and remembered
- Decide whether the site should use shorter / cleaner public-facing URLs and shared links.
- Decide whether the dashboard still has too many indicators or whether the current set is now stable enough.
- Decide whether tracking should stay private-only or support lightweight sharing.
- Decide whether the username + PIN flow should stay as-is or move to:
  - email magic link
  - Google sign-in
  - another lightweight auth option
- Decide whether the saved tag set is final or still needs another pass.
- Decide whether the current lightweight tracking-account model should remain the long-term approach or later join a broader site membership system.
- Decide whether the current frontend/backend split should stay in place or only later consolidate if it starts slowing development.
- Decide how the future assistant should behave:
  - explainer only
  - ranked recommender
  - portfolio / collection copilot
- Decide what guardrails and disclaimers are required if an assistant is added.

Future Ideas
------------

- Keep moving expensive live SQL into precomputed snapshots until the page is mostly rendering prepared data.
- Keep shifting independent dashboard sections toward parallel loading where it actually improves perceived speed.
- Add lightweight caching where safe for:
  - health
  - group signals
  - top movers
  - universe metadata
- Keep detailed card/set data loading on selection instead of preloading broad datasets.
- Keep left-rail / ticker mini-graph payloads minimal and snapshot-backed.
- Clarify the public-facing product story:
  - what am I searching for?
  - what am I buying?
  - what action should I take next?
- Make the main value proposition clearer on first view.
- Build `Buy List` into a stronger sourcing / card-show workflow with target prices and shopping-list behavior.
- Explore budget-based assistant prompts.
- Explore collection-completion prompts.
- Explore entry / exit prompts such as:
  - what looks buyable now
  - what is flattening out
  - what may have reached a resale target
- Consider a dedicated pre-breakout accumulation screen.

Future Data / Research
----------------------

- Find a condition-aware price source before building any real resale/condition layer.
- Confirm whether a future source can provide usable condition tiers such as:
  - Near Mint
  - Lightly Played
  - Moderately Played
- Confirm whether that source supports:
  - historical condition pricing
  - current condition pricing only
  - broad enough card coverage to matter
- If condition data becomes available later, revisit:
  - NM vs LP spreads
  - condition-aware buy logic
  - resale-margin math after fees and shipping
  - whether condition data belongs inside product fields or a separate valuation layer

Future Architecture
-------------------

- Revisit larger stack consolidation only if the current split becomes a real blocker.
- Evaluate whether Pokémon Momentum should eventually become:
  - an installable PWA first
  - or a native mobile app later if phone/tablet usage becomes the primary workflow
