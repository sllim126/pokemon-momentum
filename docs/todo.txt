Pokemon Momentum TODO
=====================

Active work only. Archived completed items live in `docs/todo_archive.txt`.

High Priority
-------------

- Mobile-first UI/UX refresh:
  - Source notes and the decision-card HTML reference live in `docs/mobile_redesign_notes.txt`.
  - Treat this as both a UI refresh (visual system, shared components, responsive layout) and a UX refresh (task flow, navigation, wording, speed, and confidence).
  - Rebuild around the core user questions:
    - what should I look up quickly?
    - is this card/sealed item buyable at market or below market?
    - is it likely rising, flattening, declining, or caught in hype/a spike?
    - how healthy is the overall market, and how healthy is this set?
    - what can I buy with a specific budget?
    - what is worth picking up now, including items going down while the broader market rises?
  - Use a cohesive site shell across dashboard, set explorer, sealed deals, index pages, tracking, and future assistant flows instead of standalone pages with separate navigation patterns.
  - Borrow mobile patterns intentionally from Amazon/Walmart/Yahoo Finance:
    - persistent search-first header
    - obvious category/scope switching
    - list-first results with recognizable thumbnails
    - quick price/change/sparkline summary rows
    - tap-through product detail that answers "buy, wait, avoid, or research more?"
  - Make phone the primary design target, tablet the analysis/comparison target, and desktop the power-user target.
  - Align English/Japanese and Singles/Sealed as first-class scope switches throughout the experience.
  - Make the public-facing value proposition obvious on first view: Pokemon market screening for long-term momentum without chasing hype, buyouts, or short spikes.
  - Add spike/dip suppression to price-history indicators:
    - for cards valued over `$200`, ignore single-observation moves greater than roughly `25%` up or down as likely manipulation, bad data, buyout noise, or non-repeatable market distortion
    - make the threshold configurable later by price band, liquidity, and observation count instead of hard-coding one global rule
- Mobile decision-card prototype:
  - Accomplished on 2026-04-28:
    - Promoted the mobile decision-card redesign to the live mobile/tablet `/` and `/dashboard` experience.
    - Preserved the preferred hamburger menu and account drawer patterns from the mockup.
    - Removed the old visual-card wrapper styling so the mobile shell feels more integrated with the site background.
    - Added persistent search/header shell plus obvious English/Japanese and Singles/Sealed switches.
    - Defaulted the landing feed to the current desktop `Top Movers` indicator.
    - Moved market price next to the selected item name and kept the selected item detail above the result feed.
    - Reworked the deal checker layout with seller price first, edge vs market, suggested offer, and corrected buyer/seller wording.
    - Added Stats / Graph / Insight tabs so only one supporting panel is visible at a time.
    - Made the graph functional with lazy-loaded real series data, underfill, date markers, and chart range controls.
    - Hydrated row sparklines for all visible results in safe batches instead of limiting to the first 30.
    - Added card/product images where available.
    - Added Save bottom-sheet behavior with Favorite, Watchlist, Needs Review, and Buy List options.
    - Added Full Data modal behavior for the previous full-detail style flow.
    - Improved touch targets, focus states, and thumb-friendly controls across header, drawers, rows, tabs, and bottom actions.
    - Updated the account icon, tracked-count heart badge, and heart click behavior.
    - Made the heart open all tracked items.
    - Made account menu saved-research links open filtered tracked views:
      - Tracked Items = all saved items
      - Favorites = `favorite`
      - Watchlist = `watchlist`
      - Needs Review = `research`
      - Buy List = `owned`
    - Built, then intentionally hid, the first Alerts & Targets mobile workspace while the notification strategy waits:
      - Price Targets = local buy-below / sell-above target inputs for tracked items
      - Momentum Alerts = local rule toggles for breakout, pullback, momentum shift, and stale tracking ideas
      - Recent Alerts = first-pass inbox shape using saved targets and tracked-item movement context
    - Hid the deal checker while viewing tracked/favorites lists but kept Stats / Graph / Insight visible.
    - Added URL refresh persistence for selected market, product type, feed/mode, search query, selected product, subtype, and tracked tag.
    - Mapped hamburger indicator links to mobile in-page feeds for:
      - Top Movers
      - Early Movers
      - Pullbacks
      - Under the Radar
      - Confirmed Movers
      - Consistent Movers
      - Breakouts
    - Removed the live menu link back to the Mobile Rebuild Mockup.
    - Built the mobile Feedback & Support page shell:
      - account-menu links now open Feedback, Report a Bug, and View Guide tabs
      - feedback and bug reports submit into the existing `/bug_reports` internal review queue with mobile context
      - View Guide now has a first-pass quick guide for search, scope switches, deal check, and saved lists
  - Next pass to review:
    - Decide whether tracked account lists should show English and Japanese together by default, with row-level EN/JP labels and optional list filtering.
    - Store or migrate tracking metadata so saved items reliably retain category/market, not just `productId||subTypeName`.
    - Build proper mobile flows for Browse Set, Browse Species, Set Strength, and Time to Buy instead of relying on desktop-dashboard tab concepts.
    - Make Set Explorer mobile-friendly with the same mobile shell, readable set cards, thumb-friendly filters, and quick tap-through behavior.
    - Make Sealed Deals mobile-friendly with the same mobile shell, clearer deal rows/cards, and phone-first sorting/filter controls.
    - Tidy index overview pages for mobile: improve spacing, row readability, tap targets, and index navigation without rebuilding the whole index experience yet.
    - Revisit Alerts & Targets later:
      - keep this section hidden from the account menu for now
      - site-specific/in-site notifications should be workable because market data updates daily, not continuously
      - define daily alert evaluation rules for target hits, watchlist movement, breakouts, pullbacks, stale tracked items, and sell-above targets
      - decide whether alert targets/rules should sync to the tracking account instead of localStorage
      - decide whether notification delivery should start as in-site badges/inbox only before email, Discord, PWA push, or native-app push
    - Upgrade View Guide from first-pass static help into a real guided walkthrough if users need more onboarding.
    - Mobile account follow-up: add the desktop Google sign-in block later if mobile should manage sign-in directly; for now mobile can reuse an existing tracking token and supports sign-out.
    - Review hamburger menu labels after real usage and remove anything that does not earn its spot on mobile.
    - Improve mobile search results and search suggestions beyond filtering the currently loaded feed.
    - Decide if tracked/favorites should have their own internal All / EN / JP / Singles / Sealed filter row.
    - Decide whether the deal checker should remain hidden for all tracked views or be available behind a secondary action.
    - Add better empty states for filtered tracked lists and list-specific onboarding text.
    - Confirm Full Data modal has every field/link needed from the previous mobile details page.
    - Confirm all indicator feeds use the right mobile row labels, scoring, and sorting behavior.
    - Review performance on real phones after the full-result sparkline hydration change.
    - Run a link-by-link mobile QA pass after the next migration/deployment.
- Desktop command-center redesign:
  - source notes for the desktop mockup live in `docs/desktop_redesign_notes.txt`
  - preserve desktop as the exploration / analysis product rather than forcing the mobile decision-card flow onto large screens
  - center the desktop around filters, ranked opportunity tables, and a persistent selected-product drilldown
  - reconcile proposed tabs like `Top Opportunities` and `Undervalued` with current dashboard indicators
- Mobile / tablet layout acceptance criteria:
  - iPhone 14 Pro Max: stop the bottom mobile nav from overlapping the last visible `Filter Sets` card.
  - iPhone 14 Pro Max: reduce aggressive card-row title truncation so premium-card names stay distinguishable.
  - iPad Air / iPad Pro: give tablet a dedicated analysis layout with stronger information hierarchy instead of enlarged mobile.
  - Tablet workflow: reduce the chart/list scroll ping-pong so comparing cards does not require repeated long scrolls.
  - Rebalance the selected-product hero and `Market Signals` panel so the main screener/chart stay primary on tablet.
- Mobile search/discovery UX follow-up from bug ticket `id=6` (2026-04-16):
  - review 5 leading commerce/mobile shopping layouts and document shared patterns worth borrowing
  - increase search-result thumbnail size on mobile so card recognition is faster
  - prioritize fast "find card quickly" flow over dense data in the mobile search experience
- Market-switch/search intent follow-up from bug ticket `id=5` (2026-04-15):
  - make EN vs JP market switching more obvious on mobile
  - evaluate auto-market suggestion/switch when query strongly matches market-specific sealed terms (example: EN query typed while JP is intended)
- Product classification cleanup:
  - verify product-kind rebuild results now that broad `other` classifications default into sealed instead of singles/cards.
- Make tool/page navigation more discoverable on desktop and mobile instead of hiding too much in the bottom-left account/menu area.
- Improve narrower laptop / small-desktop responsiveness so the chart, indicators, and table columns stay more obvious without requiring ultra-wide screens.

Active Queue
------------

- TCG placeholder workflow intake:
  - Treat `TCG Placeholders/placeholders.csv` as the current Scarlet & Violet source of truth and `TCG Placeholders/mega_placeholders.csv` as the Mega-era source, matching the workflow notes in `TCG Placeholders/README.md`.
  - Keep `TCG Placeholders/Placeholders.xlsx` as historical reference only unless we intentionally switch the source back to workbook-first editing.
  - Use these validation/build commands before changing or publishing placeholder outputs:
    - `python3 "TCG Placeholders/tools/build_placeholders.py" --input "TCG Placeholders/placeholders.csv" --output-dir /tmp/tcg_placeholders_check`
    - `python3 "TCG Placeholders/tools/build_checklists.py" --input "TCG Placeholders/placeholders.csv" --input "TCG Placeholders/mega_placeholders.csv" --input "TCG Placeholders/prize_pack_series7.csv" --input "TCG Placeholders/prize_pack_series8.csv" --output-dir /tmp/tcg_checklists_check --set-catalog "TCG Placeholders/data/set_release_dates.csv" --overrides "TCG Placeholders/data/checklist_overrides.csv" --additions "TCG Placeholders/data/checklist_additions.csv"`
  - Current local review result on 2026-07-02:
    - placeholder builder ran cleanly on `placeholders.csv` with 781 rows and no validation issues
    - checklist builder ran cleanly across placeholder + mega + Prize Pack 7/8 sources and produced 816 checklist rows across 18 set pages
  - Data gaps already identified by the imported review reports:
    - `TCG Placeholders/checklists_sv/svp_compare_report.md` calls out missing standard-size SVP rows like `SVP175`, `SVP176`, `SVP213`, `SVP214`, and `SVP215`
    - `TCG Placeholders/checklists_sv/mep_compare_report.md` calls out missing MEP staff variants plus future Pitch Black prerelease rows such as `MEP082`-`MEP085`
  - Decide repo hygiene before committing this folder:
    - whether generated outputs under `TCG Placeholders/output*` and `TCG Placeholders/checklists_*` should be committed or treated as build artifacts
    - whether nested metadata folders like `TCG Placeholders/.git`, `TCG Placeholders/.agents`, and `TCG Placeholders/tools/__pycache__` should be removed from the working tree or ignored
  - If we keep this workflow, add a first-class repo script or Make target later so placeholder/checklist regeneration does not live only inside the imported folder README.

- Expand Set Explorer rarity / variant filters beyond the current first pass.
- Fix any remaining generation grouping mistakes when a set clearly lands in the wrong era bucket.
- Extend tracking into:
  - `/set-explorer`
- Add keyboard shortcuts beyond the current up/down row stepping if they clearly improve desktop workflow.
- Monitor `Under the Radar` population for a few days before loosening it again.
- Add a basic Discord path for:
  - key stat lookups
  - webhook notifications
  - bug report forwarding
- Review first-load performance again after real usage and confirm the server-computed screener path actually solved the slow default loads.

Deferred / Not Now
------------------

- Keep high-resolution image support deferred until there is a stronger need than the current fast-loading image flow.
- Keep camera / photo card lookup as a later project after the main phone workflow settles.

Profiling Notes (April 2026)
----------------------------

- First-load profiling pass completed on the current dashboard boot path.
  - Container timing sample for the default card dashboard path:
    - `/series?productId=88029&subTypeName=Reverse+Holofoil&days=365&category_id=3` ~= 472 ms
    - `/health?category_id=3` ~= 227 ms
    - `/top_movers?days=30&limit=120&min_prior=5&product_kind=card&category_id=3` ~= 169 ms
    - `/sma30_holds?...` ~= 121 ms
    - `/tracking/auth_config` and `/categories` were negligible by comparison
  - Browser waterfall on a URL-selected `early_uptrends` view showed slower first paint requests:
    - `/early_uptrends?...` ~= 2.19 s
    - `/series?...` ~= 1.55 s
    - `/universe?limit=1...` ~= 1.35 s
    - `/health?category_id=3` ~= 1.27 s
- Current first-load suspects worth reducing next:
  - avoid first-paint dependence on `/health`
  - avoid metadata fallback hits like `/universe?limit=1...` when a selected row already has enough payload
  - keep heavier non-default table endpoints (`early_uptrends`, `group_products`, etc.) from blocking the first usable screen when they come from URL state

Needs Decisions
---------------

- Mobile should move to a task-based stacked layout instead of preserving the current desktop three-panel structure.
- Right-rail content should collapse into the selected product/detail flow on phone and compact tablet layouts.
- Market Signals should become the mobile home/feed layer, while the chart/product detail becomes a drilldown or comparison view.
- Decide whether the site should use shorter / cleaner public-facing URLs and shared links.
- Decide whether the dashboard still has too many indicators or whether the current set is now stable enough.
- Decide whether tracking should stay private-only or support lightweight sharing.
- Decide whether the current lightweight tracking-account model should remain the long-term approach or later join a broader site membership system.
- Decide how the future assistant should behave:
  - explainer only
  - ranked recommender
  - portfolio / collection copilot
- Decide what guardrails and disclaimers are required if an assistant is added.

Future Ideas
------------

- Keep moving expensive live SQL into precomputed snapshots until the page is mostly rendering prepared data.
- Keep shifting independent dashboard sections toward parallel loading where it actually improves perceived speed.
- Add lightweight caching where safe for:
  - health
  - group signals
  - top movers
  - universe metadata
- Keep detailed card/set data loading on selection instead of preloading broad datasets.
- Keep left-rail / ticker mini-graph payloads minimal and snapshot-backed.
- Build `Buy List` into a stronger sourcing / card-show workflow with target prices and shopping-list behavior.
- Build a flip-focused budget basket page that uses local snapshot fields like `recent_distinct_prices_30d`, ROC windows, rarity buckets, and spend caps to suggest resale-oriented buys.
- Explore budget-based assistant prompts.
- Explore collection-completion prompts.
- Explore entry / exit prompts such as:
  - what looks buyable now
  - what is flattening out
  - what may have reached a resale target
- Consider a dedicated pre-breakout accumulation screen.

Future Data / Research
----------------------

- Find a condition-aware price source before building any real resale/condition layer.
- Find a source with usable sales-volume / listing-liquidity signals before adding any real `possible buyout` flag.
- Confirm whether a future source can provide usable condition tiers such as:
  - Near Mint
  - Lightly Played
  - Moderately Played
- Confirm whether that source supports:
  - historical condition pricing
  - current condition pricing only
  - broad enough card coverage to matter
- Confirm whether a future source can provide usable buyout-adjacent signals such as:
  - sales volume
  - listing count / available quantity
  - time-windowed activity that is reliable enough to avoid false alarms
- If condition data becomes available later, revisit:
  - NM vs LP spreads
  - condition-aware buy logic
  - resale-margin math after fees and shipping
  - whether condition data belongs inside product fields or a separate valuation layer

Future Architecture
-------------------

- Revisit larger stack consolidation only if the current split becomes a real blocker.
- Evaluate whether Pokémon Momentum should eventually become:
  - an installable PWA first
  - or a native mobile app later if phone/tablet usage becomes the primary workflow
